/**
 * Playwright Fixtures Index
 * Barrel export for all fixture modules
 */

// Export auth fixtures
export { test as authTest, expect } from "./auth.fixtures";

// Export db fixtures
export { test as dbTest } from "./db.fixtures";
