apiVersion: v1
kind: Pod
metadata:
  name: {{ .Release.Name }}-test-connection
spec:
  containers:
  - name: curl
    image: curlimages/curl
    args:
    - curl
    - http://{{ .Release.Name }}:8080
  restartPolicy: Never
