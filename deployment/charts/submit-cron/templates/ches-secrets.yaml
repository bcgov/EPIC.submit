{{- if .Values.chesSecrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: ches-secrets
  labels:
    app: {{ .Values.name }}
type: Opaque
data:
  CHES_TOKEN_END_POINT: {{ .Values.chesSecrets.tokenEndPoint | b64enc }}
  CHES_BASE_URL: {{ .Values.chesSecrets.apiEndPoint | b64enc }}
  CHES_CLIENT_ID: {{ .Values.chesSecrets.clientId | b64enc }}
  CHES_CLIENT_SECRET: {{ .Values.chesSecrets.clientSecret | b64enc }}
{{- end }}
